{
  "name": "store-to-knowledge-skill",
  "version": "1.0.0",
  "description": "Convert e-commerce store into structured knowledge base",
  "entry": "SKILL.md",
  "inputs": {
    "store_url": "string",
    "output_format": {
      "type": "string",
      "enum": ["markdown", "text", "pdf"]
    }
  },
  "outputs": {
    "type": "files",
    "files": ["products", "policy", "faq"],
    "description": "Generate Products, Policy, and FAQ as separate files in the requested output format."
  }
}
